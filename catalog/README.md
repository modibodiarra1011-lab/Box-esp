# Catalogue

Le catalogue historique fourni avec le projet est conservé sous `catalog/COMPONENTS/catalog.json`.
Les dossiers importés restent sous `projects/IMPORTED_35`.
Les nouveaux composants peuvent être ajoutés avec `component.json`, `example.ino` et `wiring.md`, puis analysés avec `scripts/range_analyzer.py`.
